query GetCategories{
    categoryList {
        id,
        uid,
        level,
        name,
        url_path,
        children {
        id,
        uid,
        level,
        name,
        url_path,
        children { id,uid, level, name, url_path }
        }
    }
}